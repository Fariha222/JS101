while (true) {
	let number = Math.floor(Math.random() * 10);   //returns a random number between 0 and 9 inclusive
  console.log(number);
while (number === 5) {
	console.log("Exiting ... ...");
	break;
}
}
